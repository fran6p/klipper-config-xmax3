[delayed_gcode cool_nozzle]
initial_duration: 0 #if initial_duration is zero, the delayed gcode won't start by default
gcode:
    M106 P0 S0
    UPDATE_DELAYED_GCODE ID=cool_nozzle DURATION=0

[delayed_gcode air_filtration]
initial_duration: 0 #if initial_duration is zero, the delayed gcode won't start by default
gcode:
    M106 P3 S0
    UPDATE_DELAYED_GCODE ID=air_filtration DURATION=0	